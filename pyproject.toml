[project]
name = "metaculus-forecast-ledger"
version = "0.1.0"
description = "Read-only Metaculus forecast ledger"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
